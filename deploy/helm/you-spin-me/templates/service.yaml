apiVersion: v1
kind: Service
metadata:
  name: {{ include "ysm.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ysm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ysm.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
